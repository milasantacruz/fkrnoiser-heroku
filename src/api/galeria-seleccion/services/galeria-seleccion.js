'use strict';

/**
 * galeria-seleccion service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::galeria-seleccion.galeria-seleccion');
